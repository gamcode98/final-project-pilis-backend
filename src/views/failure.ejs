<!DOCTYPE html>
<html lang="es">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <link rel="preconnect" href="https://fonts.googleapis.com">
  <link rel="preconnect" href="https://fonts.gstatic.com" crossorigin>
  <link href="https://fonts.googleapis.com/css2?family=Roboto:wght@400;500;700&display=swap" rel="stylesheet">
  <link rel="stylesheet" href="/css/index.css">
  <title>Error en el pago</title>
</head>
<body>
  <main>
    <article class="card">
      <h1 class="title">Error en el pago</h1>
      <p class="paragraph">Lo sentimos, hubo un problema con el procesamiento de tu pago.</p>
      <img src="/assets/img/delete.gif" alt="error" class="img">
      <a href="<%= frontendUrl %>" class="link" id="web-app">Regresar a la aplicación web</a>
      <a href="<%= mobileUrl %>" class="link" id="mobile-app">Regresar a la aplicación mobile</a>
    </article>
  </main>
  <script>
    document.addEventListener('DOMContentLoaded', function () {
      const webAppLink = document.getElementById('web-app')
      const mobileAppLink = document.getElementById('mobile-app')

      if (/Android|webOS|iPhone|iPad|iPod|BlackBerry|IEMobile|Opera Mini/i.test(navigator.userAgent)) {
        webAppLink.style.display = 'none'
      } else {
        mobileAppLink.style.display = 'none'
      }
    })    
  </script>
</body>
</html>